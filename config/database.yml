default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: twitter_feeder_development

test:
  <<: *default
  database: twitter_feeder_test

production:
  url: <%= ENV['DATABASE_URL'] %>
